app:
  log-level: info
  concurrency:

gpas:
  url:

fhir:
  provider:
    mongodb:
      connection: dummyConnection
      batch-size: 10000
  pseudonymizer:
    url:
    retry:
      count: 10
      timeout: 10
      wait: 5
      max-wait: 20
